<div class="home-container-plain">

  <div class="container p-4">
  <h2 class="mb-4" > Diligencia la información para registrar tu voto</h2>

  <%= simple_form_for [:votation, @sector, @candidacy, @vote],
  wrapper: :horizontal_form,
  wrapper_mappings: {
    boolean:       :horizontal_boolean,
    check_boxes:   :horizontal_collection,
    date:          :horizontal_multi_select,
    datetime:      :horizontal_multi_select,
    file:          :horizontal_file,
    radio_buttons: :horizontal_collection,
    range:         :horizontal_range,
    time:          :horizontal_multi_select
  }  do |f| %>

    <div class="form-inputs">
      <%= f.input :voter_full_name %>
      <%= f.input :voter_tid %>
      <%= f.input :voter_email, as: :email %>
      <%= f.input :voter_phone %>

    <div class="form-actions mb-4 mt-4">
      <%= f.button :submit, "Registrar voto", class:"btn btn-primary" %>
    </div>
  <% end %>
  </div>
</div>
